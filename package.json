{
  "name": "orchidfashionschool",
  "homepage": "https://calebtolorunleke.github.io/orchidFashionSchool/",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vercel --prod && gh-pages -d dist",
    "start": "next start",
    "build:deploy": "npm run build && npm run deploy",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "preact": "^10.26.5"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "vite": "^6.3.5"
  }
}
